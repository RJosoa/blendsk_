{% extends 'base.html.twig' %} {% block body %}
<h1>Connexion</h1>
{{ form_start(form) }}
{{ form_widget(form) }}
<button type="submit">Se connecter</button>
{{ form_end(form) }}
{% endif %} {% endblock %}
